<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PESYONG.Presentation.Views.Customer.CartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PESYONG.Presentation.Views.Customer"
    xmlns:models="using:PESYONG.Domain.Entities.Orders"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="20">
        <Grid Background="#FAFAFA" Padding="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="20">
                <TextBlock Text="Your Cart" FontSize="28" FontWeight="Bold" Foreground="#F97316"/>

                <ItemsControl ItemsSource="{x:Bind ViewModel.CurrentOrder.OrderItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:OrderMealProduct">
                            <Border Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid ColumnDefinitions="Auto, *, Auto">
                                    <StackPanel Grid.Column="1" Margin="15,0">
                                        <TextBlock Text="{x:Bind MealProduct.ProductName}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind ItemPrice, Converter={StaticResource CurrencyConverter}}" Foreground="Gray"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{x:Bind SubTotal, Converter={StaticResource CurrencyConverter}}" VerticalAlignment="Center" FontWeight="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Background="#FFF7ED" CornerRadius="12" Padding="20" BorderBrush="#FFEDD5" BorderThickness="1">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Delivery Details" Foreground="#9A3412" FontWeight="Bold"/>
                        <TextBox Text="{x:Bind ViewModel.CurrentOrder.Address, Mode=TwoWay}" Header="Address" PlaceholderText="123 Main St..."/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="25" VerticalAlignment="Top" Margin="20,50,0,0">
                <StackPanel Spacing="15">
                    <TextBlock Text="Summary" FontSize="20" FontWeight="Bold"/>
                    <Grid ColumnDefinitions="*, Auto">
                        <TextBlock Text="Delivery Fee" Foreground="Gray"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.DeliveryFee, Converter={StaticResource CurrencyConverter}}"/>
                    </Grid>
                    <MenuFlyoutSeparator/>
                    <Grid ColumnDefinitions="*, Auto">
                        <TextBlock Text="Total" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.Total, Converter={StaticResource CurrencyConverter}}" Foreground="#F97316" FontSize="18" FontWeight="Bold"/>
                    </Grid>
                    <Button Content="Place Order" Background="#F97316" Foreground="White" HorizontalAlignment="Stretch" Height="45" Margin="0,15,0,0" CornerRadius="8"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
